# API Contract

Canonical contract for Stage 3 OpenAPI: **`ari-orchestrator/CONTRACT.md`** (branch `feature/p1-p2-api-contract`).

Brain (`Collection`) implements the caller side and transcript error envelope per that document.
